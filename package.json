{
  "name": "root",
  "private": true,
  "dependencies": {
    "lerna": "^3.19.0",
    "serverless": "^1.68.0",
    "serverless-domain-manager": "^3.3.2",
    "serverless-dotenv-plugin": "^2.4.2",
    "serverless-offline": "^6.1.4",
    "serverless-plugin-typescript": "^1.1.9",
    "netlify-cli": "^2.41.0",
    "prettier": "^1.19.1",
    "tslint": "^5.20.1",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.1.0",
    "tslint-react": "^4.1.0",
    "tslint-react-hooks": "^2.2.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna run tsc && lerna publish",
    "storybook": "lerna run storybook --stream",
    "tsc": "tsc",
    "prebuild": "lerna bootstrap",
    "build": "lerna run build-ui && lerna run build-graphql && lerna run build-login && lerna run build-media && lerna run build-fe && lerna run build-server",
    "publish-fe": "netlify deploy --dir=packages/front-end/build/ -p",
    "publish-server": "sls deploy -c packages/server/serverless.yml",
    "1-start-lambda": "lerna run start-server --stream",
    "2-watch-packages": "lerna run build:watch --stream",
    "3-start-app": "lerna run start-front-end --stream"
  }
}
