{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^12.7.11",
    "lerna": "^3.16.4",
    "prettier": "^1.18.2",
    "tslint": "^5.20.0",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.0.1",
    "typescript": "^3.6.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish": "lerna run tsc && lerna publish",
    "storybook": "lerna run storybook --stream",
    "tsc": "tsc",
    "watch-dependencies": "lerna run build:watch --stream",
    "start-server": "lerna run start-server --stream",
    "start-front-end": "lerna run start-front-end --stream"
  }
}
